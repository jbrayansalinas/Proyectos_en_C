/*#include<stdio.h>
#include<conio.h>

int retornarSuperficie(int lado){

    int superficie=lado*lado;
    return superficie;
}

int main(){

    int valor;
    int sup;
    printf("Ingrese el valor del lado del cuadrado: ");
    scanf("%i", &valor);
    sup=retornarSuperficie(valor);
    printf("La usperficie del cuadrado es %i", sup);
    getch();
    return 0;
}
*/
